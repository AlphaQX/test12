#include <stdio.h>

int main() {
    int a, b;

    printf("Enter the first numbers: ");
    scanf("%d", &a);

    printf("Enter the second number now: ");
    scanf("%d", &b);

    printf("Sum = %d\n", a + b);

    return 0;
}
